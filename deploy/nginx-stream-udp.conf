# UDP ICE. Forward to the host that runs LiveKit rtc.udp_port (21116).
# Replace the address with yours.

server {
    listen 21116 udp;
    listen [::]:21116 udp;
    proxy_pass 203.0.113.10:21116;
    proxy_timeout 24h;
    proxy_responses 1;
}
